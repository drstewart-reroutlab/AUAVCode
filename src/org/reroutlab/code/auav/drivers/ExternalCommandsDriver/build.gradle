apply plugin: 'java'
jar.baseName = 'org.reroutlab.code.auav.drivers.ExternalCommandsDriver'

repositories {
   flatDir {
     dirs '../../../../../../../libs'
   }
}

dependencies {
   compile name: 'interfaces'
	 }
